#!/bin/bash
# Задание 2: вывести PID процесса с наибольшим временем непрерывной работы
# etimes — прошедшее время в секундах; наибольшее значение = дольше всего работает
ps -eo pid,etimes --no-headers | sort -k2 -nr | awk 'NR==1 {print $1}'
